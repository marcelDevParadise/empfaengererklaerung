<?php
// Deliberately preserve declarations, settings and capabilities when uninstalling.
// Administrators can explicitly delete declarations (including PDFs) in the archive.
if (!defined('WP_UNINSTALL_PLUGIN')) { exit; }
